Guarda aqui tus modelos YOLO locales, por ejemplo:

- `yolov8n.pt`
- `mi_modelo.pt`
- trackers `.yaml` personalizados

Si en `config/yolo_params.yaml` usas una ruta relativa, puedes apuntar asi:

- `model: models/mi_modelo.pt`
- `tracker: models/mi_tracker.yaml`
